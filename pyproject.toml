[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [
  ".git",
  ".pytest_cache",
  ".ruff_cache",
  "__pycache__",
  "build",
  "converted_pptx",
  "skill",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
