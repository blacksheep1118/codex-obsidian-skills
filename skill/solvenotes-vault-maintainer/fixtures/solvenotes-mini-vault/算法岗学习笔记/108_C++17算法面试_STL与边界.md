---
course: "算法岗学习笔记"
note_type: "course_note"
source_files: []
coverage: "checked"
last_checked: "2026-08-20"
tags:
  - "course/算法岗学习笔记"
---

# C++17 算法面试

代表性 runnable block 必须自包含，不能依赖另一段代码的头文件。

<!-- runnable: cpp17 -->
```cpp
#include <algorithm>
#include <iostream>
#include <vector>

int main() {
    const std::vector<int> values{3, 1, 2};
    const auto it = std::max_element(values.begin(), values.end());
    std::cout << *it << '\n';
}
```
