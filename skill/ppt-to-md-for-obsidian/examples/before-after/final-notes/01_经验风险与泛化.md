# 经验风险与泛化

经验风险衡量模型在训练样本上的平均损失：

$$
R_{emp}(f) = \frac{1}{n}\sum_{i=1}^{n} L(f(x_i), y_i)
$$

其中 $f$ 是模型，$x_i$ 是第 $i$ 个样本，$y_i$ 是标签，$L$ 是损失函数。经验风险低并不必然代表测试表现好；当模型只记住训练集细节时，就会出现过拟合。

泛化能力关注模型在未见样本上的表现。验证集、正则化和交叉验证都是估计或提升泛化能力的常见手段。
